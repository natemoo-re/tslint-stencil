import { Component, Prop } from '@stencil/core';

// Pass: No hostData or render
@Component({
    tag: 'my-element-a'
})
export class ElementA {
    someFunction() { }
}

// Pass: hostData, but no render
@Component({
    tag: 'my-element-b'
})
export class ElementB {
    hostData() { }
}

// Pass: hostData immediately precedes render with only two members
@Component({
    tag: 'my-element-c'
})
export class ElementC {
    hostData() { }
    render() { }
}

// Pass: hostData immediately precedes render with other members
@Component({
    tag: 'my-element-d'
})
export class ElementD {
    @Prop() propA: string;
    @Prop() propB: string;
    @Prop() propC: string;

    hostData() { }
    render() { }
}

// Fail: hostData follows render with only two members
@Component({
    tag: 'my-element-e'
})
export class ElementE {
    render() { }
    hostData() { }
    ~~~~~~~~ [failure]
}

// Fail: hostData precedes render by more than one
@Component({
    tag: 'my-element-f'
})
export class ElementF {
    hostData() { }
    ~~~~~~~~ [failure]
    @Prop() propA: string;
    @Prop() propB: string;
    @Prop() propC: string;
    render() { }
}

[failure]: hostData should immediately precede the render method