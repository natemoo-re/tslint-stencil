import { Component, Prop } from '@stencil/core';

@Component({
    tag: 'my-component'
})
export class ElementA {
    
    ownProp = false;

    @Prop() mySingleProp: string;

    @Prop()
    ~~~~~~~
    myMultiProp: string;
~~~~~~~~~~~~~~~~~~~~~~~~ [failure]
}

[failure]: Variable decorators should be inlined